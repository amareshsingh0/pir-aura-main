import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.piraura.app',
  appName: 'PIR Motion Live Dashboard',
  webDir: 'dist',
};

export default config;
